# Προσωπικός Agent — Οδηγίες εγκατάστασης

## Τι είναι

Web app (PWA) που εγκαθίσταται στο κινητό σαν κανονική εφαρμογή, με:

- **💳 Πληρωμές** — υποχρεώσεις με ημερομηνίες, ποσά, επαναλαμβανόμενες
- **📚 Βιβλία** — add / edit / delete, καταστάσεις, βαθμολογία
- **📝 Σημειώσεις** — ελεύθερο κείμενο
- **💬 AI Agent** — του λες κάτι, το βάζει μόνος του στη σωστή κατηγορία
- **☁️ Google Drive sync** — τα δεδομένα σε αρχείο `personal-agent-data.json` στο Drive σου
- **💾 Export / Import** — τοπικό backup σε JSON

---

## Αρχεία

| Αρχείο | Τι κάνει |
|---|---|
| `index.html` | Η εφαρμογή |
| `manifest.json` | Για να εγκαθίσταται σαν app |
| `sw.js` | Service worker (offline) |
| `icon-192.png` | Εικονίδιο |
| `icon-512.png` | Εικονίδιο |

**Και τα 5 πρέπει να είναι στον ίδιο φάκελο.**

---

## Βήμα 1 — Ανέβασμα στο GitHub Pages

1. Πήγαινε στο https://github.com και κάνε login (ή δημιούργησε λογαριασμό)
2. Πάνω δεξιά **+** → **New repository**
3. Όνομα: `agent` · διάλεξε **Public** · πάτα **Create repository**
4. Στη σελίδα που ανοίγει: **uploading an existing file**
5. Σύρε μέσα και τα **5 αρχεία** → **Commit changes**
6. Πάνω: **Settings** → αριστερά **Pages**
7. Στο *Branch* διάλεξε **main** / **root** → **Save**
8. Περίμενε 1–2 λεπτά. Θα εμφανιστεί το link σου:

```
https://ΤΟ-USERNAME-ΣΟΥ.github.io/agent/
```

Κράτα αυτό το link.

---

## Βήμα 2 — Πες στο Google το νέο link

Το Google δέχεται σύνδεση μόνο από διευθύνσεις που έχεις δηλώσει.

1. https://console.cloud.google.com → **APIs & Services** → **Credentials**
2. Κλικ στο OAuth client σου (`Personal Agent Web`)
3. Στο **Authorized JavaScript origins** → **ADD URI**:

```
https://ΤΟ-USERNAME-ΣΟΥ.github.io
```

⚠️ Μόνο το domain — **χωρίς** `/agent/` στο τέλος.

4. **SAVE** (μπορεί να θέλει έως 5 λεπτά να ενεργοποιηθεί)

---

## Βήμα 3 — Εγκατάσταση στο κινητό

1. Άνοιξε το link στο **Chrome** του κινητού
2. Μενού **⋮** → **Add to Home screen** / **Εγκατάσταση εφαρμογής**
3. Θα εμφανιστεί εικονίδιο στην αρχική οθόνη — ανοίγει σαν κανονική app

---

## Βήμα 4 — Πρώτη ρύθμιση μέσα στην app

**Καρτέλα ⚙️ Ρυθμίσεις:**

1. **Claude API Key** — κόλλα το κλειδί από https://console.anthropic.com → **Αποθήκευση**
   (μένει μόνο σε αυτή τη συσκευή)
2. **Google Drive** → **Σύνδεση με Google** → δώσε έγκριση

Έτοιμο. Από εδώ και πέρα κάθε αλλαγή ανεβαίνει αυτόματα στο Drive.

---

## Πώς μιλάς στον agent

Δεν χρειάζεται εντολές — γράψε φυσικά:

- *"ΔΕΗ 145 ευρώ στις 25 Σεπτεμβρίου"* → μπαίνει στις Πληρωμές
- *"Ενοίκιο 400 κάθε μήνα στις 5"* → επαναλαμβανόμενη πληρωμή
- *"Διαβάζω το Sapiens του Harari"* → μπαίνει στα Βιβλία
- *"Να καλέσω τον λογιστή τη Δευτέρα"* → μπαίνει στις Σημειώσεις
- *"Τι εκκρεμεί αυτή την εβδομάδα;"* → απαντάει

**Για φωνή:** πάτα το πεδίο κειμένου και μετά το 🎤 του πληκτρολογίου (Gboard). Το Android κάνει τη μεταγραφή και ο agent παίρνει το κείμενο.

---

## Σημαντικά

**Ασφάλεια κλειδιού:** Το Claude API key μένει στο `localStorage` του κινητού και στέλνεται μόνο στο `api.anthropic.com`. Επειδή όμως το repo είναι public, **μη γράψεις ποτέ το κλειδί μέσα στον κώδικα** — μόνο από τις Ρυθμίσεις.

**Client Secret:** Αυτή η έκδοση **δεν** χρησιμοποιεί το Client Secret — δεν χρειάζεται καθόλου. Επειδή το είχες στείλει σε συνομιλία, καλό είναι να πας στο Google Cloud Console → Credentials → OAuth client → **Reset secret**.

**Σύνδεση Google:** Κρατάει όσο η συνεδρία. Αν σου ζητήσει ξανά σύνδεση μετά από ώρες, είναι φυσιολογικό — ένα πάτημα.

**Αν χαθεί το κινητό:** Τα δεδομένα είναι στο Drive. Ανοίγεις το link σε νέα συσκευή, συνδέεσαι με Google, φορτώνονται όλα.
